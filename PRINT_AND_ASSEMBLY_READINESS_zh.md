# Zeroth-01 round-v1 打印与装配就绪度

## 结论

不能把当前公开 Zeroth-01 模型“全部打印后直接拼成可行走机器人”。

可以直接进入切片评估的是本次新增的 11 个 round-v1 件：10 个安装件和 1 个关节孔径试片。它们的 STEP 为闭合正体积 B-Rep；最终 STL 已去除 STEP 三角化产生的零体积孤立面，11/11 均通过单组件、闭合、流形、绕序一致和 STEP/STL 体积一致性门禁。

这些件的用途是：

- 胸、头、骨盆前后壳和面部徽章：非承力外观件。
- 左右鞋底：8 mm 加厚的接触/配合原型；每只带两组 12 mm 魔术贴绑带槽，不需要在上游脚件钻孔。
- 关节环：孔径、外观和打印收缩试片，不默认装到机器人上。

它们不替代上游缺失的承力零件：舵机耳座、输出盘/花键连接、反侧轴承、金属紧固件、线束夹、限位结构和散热路径。

## 为什么原 SolidWorks 看起来像很多碎片

上游 `zeroth-sim` 提供的是 17 个 link 级聚合 STL。每个 STL 可能把舵机、支架和外壳的外表面合并在一个 link 文件里，并且存在开边、非流形边或不一致绕序。SolidWorks 以“表面体 + 三角边”导入后，视觉上会像大量碎片；这不代表存在同数量的可制造零件，也不能据此生成紧固件 BOM。

新的 SolidWorks round-v1 装配采用阴影模式隐藏三角边，并明确分成：

- 17 个上游运动 link 表面组件；
- 10 个本次新增的原生 B-Rep 外观/鞋底组件；
- 16 个真实 FEETECH STS3250 原生多实体 `SLDPRT` 实例。

因此 SolidWorks 中现在可以直接找到 `FEETECH_STS3250.SLDPRT`，不再只靠文字元数据表示舵机。

## 打印门禁

| 门禁 | 结果 | 证据 |
|---|---:|---|
| 11 个 STEP 为闭合正体积实体 | PASS | `generated/cad/round_v1/parts/` |
| 11 个最终 STL 为单一闭合流形 | PASS | `reports/round_v1_print_mesh_gate.json` |
| 单位为 mm | PASS | 同上 |
| 名义 PETG 质量/质心/惯量 | PASS（仿真值） | `generated/config/round_v1_mass_properties.json` |
| 明确打印机/喷嘴/材料/profile | BLOCKED | 用户尚未冻结切片配置 |
| G-code | BLOCKED | 禁止在未知 profile 下生成并声称可用 |
| 承力步行结构 | FAIL/未交付 | 上游生产 B-Rep、接口尺寸和结构验证缺失 |

## 建议装配顺序

1. 先打印 `ZEROTH01_ROUND_V1_JOINT_RING.stl`，验证 3.4 mm M3 通孔和材料收缩。
2. 只打印一只鞋底，低速装入对应脚件；检查 0.6 mm/侧名义配合、踝部 2.97 mm 名义静态竖向间隙和绑带路径。
3. 每只鞋底穿两条 12 mm 魔术贴绑带，做拉脱、扭转和反复弯曲试验；未通过前禁止行走。
4. 分别试装胸、头、骨盆前后壳；用候选 M3×16 螺栓、垫片和锁紧螺母连接 12 处分缝 boss。
5. 检查肩/髋极限姿态、线束弯曲半径、螺钉头和舵机外壳实际 clocking。
6. 称量每个打印件和最终整机，回填质量；测量质心或至少进行吊线/摆法复核。
7. 完成 16 个舵机 ID、零位、方向、硬限位和回差标定后，才允许低扭矩台架联动。

## 仍需补齐的硬件信息

- Onshape 原生产 B-Rep，或逐件实测的舵机支架/输出盘/反侧轴承尺寸；
- STS3250 输出花键、舵盘孔系、紧固扭矩和每个关节壳体 clocking；
- 金属轴、轴承、螺钉、螺母、垫片、热熔螺母的最终规格；
- 线束、连接器、控制板、电池和散热件的质量、位置、包络；
- 打印机、喷嘴、层高、壁数、材料批次、方向、支撑和退火策略；
- 静载、冲击、疲劳、拉脱、热和跌落试验。

只有这些数据闭环后，`hardware_deployment_ready` 才能从 `false` 改为 `true`。
