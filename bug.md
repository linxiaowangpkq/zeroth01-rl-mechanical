# Zeroth-01 v5 机械基线 / RL 问题清单

最后更新：2026-08-10

适用分支：`codex/zeroth01-v5-16dof-solidworks-motion`

RL 动力学基线：`c5cb2c7eb892579c273e68f0436e6285e0782b34`

本次复测没有修改几何、质量、COM、惯量、碰撞体、关节轴、关节限位或 `1.2552512 N·m` 连续扭矩上限。模型为 `nq=23`、`nv=22`、`nu=16`、总质量 `2.745758514949 kg`；策略观测 46 维、动作 16 维，手臂和腿均由 PPO 输出。全部训练使用 RTX 4070 Laptop / JAX CUDA。

## 历史问题状态

| 编号 | 状态 | 结论 |
|---|---|---|
| BUG-RL-V5-001 actuator / qpos 顺序不一致 | CLOSED | 生成器按最终运动树显式生成并校验 16/16 映射。 |
| BUG-RL-V5-002 `symmetric_crouch` qpos 顺序错误 | CLOSED | `gait_neutral` 按 joint name 生成，限位、双脚接地和镜像门禁通过。 |
| BUG-RL-V5-003 足底 front/rear 语义反向 | CLOSED | 足底 site 已按世界坐标语义更正并自动检查。 |
| BUG-RL-V5-004 Git LFS 离线交付不完整 | CLOSED FOR GIT CLONE | README 已要求 `git lfs pull`；GitHub ZIP 仍不能视为完整离线包。 |
| BUG-RL-V5-005 官方零位把膝踝放在硬限位 | DIGITAL CLOSED / PHYSICAL HOLD | 新 `gait_neutral` 提供双向余量；实体零位、方向、硬挡仍需首件确认。 |
| BUG-RL-V5-006 canonical MJCF 哈希契约不一致 | CLOSED | MJCF 生成器强制写入 LF 字节；compile gate、RL handoff、actuator layout 与 delivery manifest 使用同一 SHA-256。 |
| BUG-RL-V5-007 STS3250 膝关节速度不足以复现当前跑步 | OPEN / P0 FOR REAL RUNNING | 见下文。 |

## BUG-RL-V5-006：canonical MJCF 哈希契约不一致（已关闭）

根因是 Windows 工作树中 MJCF 曾以 CRLF 写出，而 delivery manifest 对文本使用 LF 规范化；compile gate 和下游 JSON 当时哈希了工作树原始 CRLF 字节，导致同一 XML 出现两个摘要。

现在 MJCF 生成器通过二进制写入强制 LF，并在写出后拒绝任何 `CR` 字节；compile gate 记录 `sha256_exact_lf_bytes`，RL handoff、actuator layout 和 delivery manifest 由该值重生成。当前规范摘要为：

```text
1354289e37aabff35d3ceec91412df367e4e4e78d3d5185dcb3624cc38fba0b2
```

关闭门禁：工作树 MJCF 不含 CR；上述三个派生文件及 delivery manifest 摘要一致；`verify_v5_delivery_manifest.py` 全项通过。

## BUG-RL-V5-007：当前跑步依赖超过 STS3250 包络的快速膝伸展

### 结论

当前机械模型本身可以稳定站立、步行和产生飞行相，不支持“先减重才能训练”的判断。仿真跑步门禁已经通过，但当前跑姿不能部署到声明最大输出速度为 3 rad/s 的 STS3250。若实机必须跑步，需要机械/CAD 会话修改膝关节执行器或传动合同；若保留 STS3250，则应把一期范围冻结为步行，并重新定义较低速度、无飞行相的硬件门禁。

### 可复核证据

1. `Z4_robust_run` 在无限速位置目标模型中通过 8/8 个 4 s 回合：平均速度 `0.150424 m/s`，最慢回合 `0.139760 m/s`，单脚支撑 `0.27625`，飞行相 `0.03`，接触滑移 `0.042263 m/s`。
2. `L3_long_run` 通过 8/8 个 10 s 生存回合，平均速度 `0.155114 m/s`、飞行相 `0.038`，但左右膝 p95 速度分别为 `5.028` / `5.024 rad/s`，最大 `7.088` / `6.913 rad/s`，超过 3 rad/s 的样本占 `28.6%` / `28.8%`。
3. L3 左右膝 RMS 扭矩为 `0.932` / `0.935 N·m`，扭矩饱和比例 `39.3%` / `39.1%`；估算 RMS 电流 `0.799` / `0.801 A`。估算母线峰值电流 `7.222 A`、最低电压 `11.639 V`。当前失败项是速度，不是总质量、RMS 电流或母线压降。
4. 对 2,000 个参考步态做 4 种子 × 10 s 搜索：87 个候选满足膝 p95 ≤3 rad/s，但没有任何候选同时存在飞行相；出现飞行相的稳定候选中，最低膝 p95 仍为 `3.191 rad/s`，且速度只有 `0.071 m/s`、飞行占比 `0.001`。
5. v13 软速度约束 PPO：16 个更新、120,001 样本。全部 checkpoint 8/8 生存并保留飞行相，但速度仅约 `0.095–0.096 m/s`，膝 p95 仍约 `4.076–4.162 rad/s`，联合门禁 0/16。
6. 对原 Z4 强制目标位置每 20 ms 最多变化 0.06 rad 后，膝 p95 降至 `2.635 rad/s`，但 0/8 回合生存且飞行相为 0。
7. v14 在同一硬 3 rad/s 目标速率模型中训练 24 次更新、147,201 样本。12 个保存点的膝 p95 为 `2.713–2.823 rad/s`，但全部 0/8 生存，飞行相最高只有 `0.0025`。后期更高的“平均速度”来自倒地前前冲，不是有效跑步。

证据文件位于 `reports/rl_training/v5_16dof_20260809/`。其中硬目标速率是保守的执行器近似，不替代实测扭矩-速度曲线；但它已经证明当前跑姿对高速膝伸展存在结构性依赖。

### 必须由机械/CAD 会话做出的决定

1. 冻结需求：一期是“可步行”还是“必须有飞行相的跑步”。两者不能继续共用同一个 STS3250 合格声明。
2. 若必须跑步：膝关节输出至少覆盖当前 `5.1 rad/s` p95，并留工程余量；建议把动态输出目标设为不低于 `6 rad/s`，同时用实测扭矩-速度曲线证明所需输出扭矩，而不是引用空载转速。
3. 更换执行器或改变传动比后，必须同步更新安装 STEP、干涉/螺钉/线束空间、质量、COM、全惯量、碰撞体、URDF、MJCF 和 actuator config。增速传动会损失输出扭矩，不能只改配置数字。
4. 若必须保留 STS3250：当前 Z4/L3 均不得上机。需要从新的机械/执行器合同重新训练，并在 8 × 10 s 中同时满足生存、速度、飞行相、滑移以及每个膝关节 p95 ≤3 rad/s；若无法满足，应将产品要求降为快走而不是把快走命名为跑步。
5. 采购至少一个真实 STS3250，测量输出端扭矩-速度-电流、死区、背隙、延迟、热阻/热时间常数和母线内阻；完成实体零位、方向、ID、硬挡和线束全行程标定。
6. 完成原生 SolidWorks Motion 和实体首件门禁。当前 digital baseline 为 PASS，但 native motion 和 physical first article 仍为 HOLD。

### 机械修改后的回归门禁

- `nq=23`、`nv=22`、`nu=16` 与 46→16 策略合同不变，或显式版本升级；
- 60 s 中立姿态无 NaN、穿地和动力学爆炸；
- N/M/Z checkpoint 均能恢复；
- 跑步 8 × 10 s 全部存活，平均速度 ≥0.15 m/s，最慢回合 ≥0.10 m/s，单脚支撑 ≥0.15，飞行相 ≥0.02，接触滑移 ≤0.05 m/s；
- 每个关节 p95 速度和扭矩-速度工作点位于实测连续包络内；
- RMS 电流、温升、母线压降和足底冲击通过实测参数复算。

没有执行任何实体舵机命令。
