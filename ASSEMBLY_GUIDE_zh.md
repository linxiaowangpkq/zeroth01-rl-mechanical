# Zeroth-01 v5 原始 16DoF 首件装配指南

本指南用于制造验证，不代表“整套直接打印即可一次装好”。数字 B-Rep、URDF/MJCF 已通过；SOLIDWORKS 当前 manifest 的原生 Motion、采购件公差、打印强度、线束和动态负载仍为 HOLD。

## 1. 先做四类试片

1. 采购一只 FEETECH STS3250-C001，对照 `parts/sts3250_step_parts_exact_shaft_frame.step` 验证 45.22×24.72×约 36.5 mm 包络、后盖 M2 孔、25T/5.9 mm 输出和 PCD14 4×M3。
2. 打印一侧髋 yaw 躯干座、U 形髋避让区、PCD14 输出桥和 M3 套筒；以 0.20–0.35 mm 为起始工艺间隙，不把 CAD 名义零间隙当打印公差。
3. 打印一个两 M3 小腕端和左右 9 mm 脚底；验证螺钉头、工具可达和脚底接地平面。
4. 做热熔嵌件拉拔、层间剪切、重复拆装和低速堵转保护试验，再冻结 PETG/PA-CF/PC、壁厚、层向和螺钉扭矩。

## 2. 关节力矩链验收

每个 S01–S16 必须同时满足：

1. STS3250 机壳属于静止侧 carrier，不能悬在外观壳或标注线上。
2. 舵机轴线与 actuator layout 中的轴线重合，左右关节由同一账本镜像生成。
3. PCD14 四孔输出桥属于运动侧 carrier；拉动运动侧时，输出桥必须与运动侧一起转。
4. 静止侧 carrier、舵机壳体、内部齿轮/输出、PCD14 桥、运动侧 carrier 构成连续力矩链。
5. 原生 SOLIDWORKS 中只允许绕该轴转动并受上下限 mate 约束；不得用漂浮组件或动画变换伪造。

## 3. 髋 yaw 专项顺序

1. 躯干使用 `torso_mounted_solid.step`，其中左右 2×M2 窄肋/螺柱已与固定躯干融合，不另装背板。
2. 把精确 STS3250 放入左右近贴合槽位；每侧从服务侧装入 2 根 M2×8。螺钉必须穿过融合螺柱并进入采购舵机后盖孔。
3. 输出侧装 2.05 mm PCD14 桥，再用四个独立 M3 套筒闭合到 `U_HIP_*_MOUNT_CLEARANCED.step`。
4. 手动从下限慢转到上限，确认固定螺钉/肋与 U 形髋不接触；数字证据见 `hip_yaw_dynamic_mount_screen.json` 和 `hip_yaw_uhip_mount_clearance.json`。
5. 不安装整圆垫片、悬空桥板、黄色背板或额外踝舵机。

## 4. 上肢、头部与脚

- 肩、肘沿原 Zeroth carrier 顺序安装；左右支架必须镜像，舵机不能通过偏置外观件补偿。
- 腕端仅装两 M3 小型圆角支撑，不装夹爪、大球手或方块手掌。
- 头罩前后两片上下左右各扩 5 mm，以四 M3 隐藏螺母板直接连躯干，外露间隙不超过 5 mm；UnitV2 可从内部托架拆出，保留摄像头、麦克风、USB-C 和散热通道。
- 脚底白色、9 mm、上窄下宽；四 M3 通孔连接原脚。黑色鞋底和踝 roll 舵机均不存在。

## 5. 电气与线束

- 计算板最大预留 70×12×32 mm；电池最大 75×22×34 mm；IMU 最大 32×8×25 mm；UnitV2 48×18.5×24 mm。
- 这些彩色包络必须在躯干/头罩内部，不得作为外露背板。精确采购件冻结后再改托架孔位。
- 舵机总线、动力、USB 和传感线分层走线；每个活动关节留服务环和应变释放，线束不得跨剪切面。
- 逐只上电低速 jog，填写 hardware calibration CSV；候选总线 ID 不具权威性。

## 6. 放行顺序

1. 32 次原生 SOLIDWORKS 单关节上下限 Motion、静态/动态干涉 PASS。
2. 无电实物全行程和镜像检查 PASS。
3. 低压悬吊单关节、限流站立、真实线束慢扫 PASS。
4. 称量整机与各 link，摆振/阶跃/扫频 SysID，覆盖质量、COM、全惯量、摩擦、背隙、延迟和扭矩增益。
5. RL 输出峰值/RMS 扭矩、电流、温升、足底接触、跌倒和紧固件松动闭环后，才可把 `physical_first_article` 改为 PASS。
