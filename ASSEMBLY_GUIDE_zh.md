# Zeroth-01 v4 真实连接版首件装配指南

本指南是首件验证流程，不是“全部打印后一定能一次装好”的工厂签字。数字装配、URDF/MJCF 和干涉门禁已通过；材料、层向、嵌件强度、采购件公差、线束和动态负载未实测，因此物理首件仍为 HOLD。

## 0. 先做试片

1. 采购一只真实 FEETECH STS3250-C001，与 `generated/cad/physical_mount_v4_original_minimal/parts/sts3250_step_parts_exact_shaft_frame.step` 对比总包络、4×M2 机壳孔、25T/5.9 mm 输出、PCD14 4×M3 和中心 M3×6。
2. 先打印一组肩/髋槽位、PCD14 输出桥、1 mm 垫环和直接踝支架试片，验证 0.2–0.35 mm 打印间隙、螺钉头、线束口和工具可达性。
3. 做嵌件拉拔、层间剪切和重复拆装测试，再冻结 PETG/PA-CF/PC、壁厚、层向和扭矩。

## 1. 当前安装件边界

- 保留：原始 Zeroth-01 承力躯干、肩/臂、髋/腿、原始脚和原始轻量腕端。
- 新增：两片头罩、UnitV2 托架、无脖子 M3 螺母板、左右 18 mm 缩短下腿、左右直驱踝 roll 支架、18 个精确 STS3250、18 个 2.05 mm PCD14 输出桥、必要的输出垫环/拉杆。
- 明确不存在：方块手掌、夹爪、球形手、外挂后背板/后勤舱、额外黑色 7 mm 鞋底、远置皮带踝、髋部整圆实体垫片。
- 计算板、电池、IMU 和线束最终放入躯干；在实际器件冻结前不打印虚构托盘。

## 2. 关节通用力矩链

每个 S01–S18 都按同一原则检查：

1. 舵机机壳通过 4×M2 固定到该关节的静止侧承力件，不能悬在外观壳上。
2. 输出轴与 `generated/config/physical_mount_v4_original_minimal_actuator_layout.json` 的轴线重合。
3. 输出盘通过 PCD14 4×M3 和中心 M3×6 连接 2.05 mm 输出桥。
4. 输出桥/垫环连接关节运动侧承力件，形成“静止侧—机壳—齿轮—输出盘—运动侧”的连续力矩链。
5. 左右件必须镜像安装；不能靠偏置舵机或增加装饰板补偿。

## 3. 髋 yaw 特殊连接

原始躯干与采购版 STS3250 外壳在轴向原位会碰撞，因此左右髋 yaw 舵机沿各自原关节轴外移 4.00 mm，轴线本身不变：

- 机壳侧：4 根 M2 螺钉跨过 4.00 mm 间距并进入原躯干孔位。
- 输出侧：2.05 mm PCD14 桥加 1.95 mm 的 4 根 M3 拉杆闭合 4.00 mm 尺寸链。
- 不安装整圆垫片或后背板。SolidWorks 原始干涉中的 8 个小体积交叠仅是这些 M3 螺钉进入采购 STEP 简化内螺纹的预期啮合；其他物理干涉为 0。

## 4. 下肢顺序

1. 装髋 yaw/roll/pitch，再装大腿、膝和缩短下腿；下腿只在直中段缩短，两端孔位不二次修改。
2. 装踝 pitch，再装左右镜像的直接踝 roll 支架和 S17/S18。检查左右舵机方向、输出桥、支架耳板和原始脚的镜像关系。
3. 不安装黑色增高鞋底。以原始脚底为接触面，确认中立位双脚同时落地，踝舵机不穿过支架或脚壳。

## 5. 上肢和头部

1. 装肩和肘，确认肩舵机机壳落在躯干/肩承力件上，输出侧连接上臂；不得出现肩件悬空。
2. 腕端保留原始轻量终端，不装方块手、夹爪或额外手掌。
3. UnitV2 装入可拆托架，确认相机、麦克风、USB-C 和散热通道可达；头罩用隐藏 M3 螺母板直接连躯干，外观无脖子。

## 6. 电气、标定与放行

1. 冻结真实计算板、2S 电池/BMS、IMU、保险丝、急停和稳压后，再绘制躯干内托盘与线束夹；当前受控包络仅用于 RL 质量预算，不能作为打印签字。
2. 逐只舵机上电并低速 jog，实测总线 ID、机械零位和方向，写入部署覆盖文件；不得把生成 JSON 的占位值当实测值。
3. 称量整机和可拆 link，用摆振/SysID 更新质量、质心、惯量、摩擦、阻尼和电机强度。
4. 依次完成无电全行程、悬吊单关节、镜像关节、带真实线束的 64 姿态慢速扫掠、限幅站立，再进入步行/跑步。
5. 只有峰值/RMS 扭矩、电流、温升、足底接触、跌倒统计和紧固件松动均闭环，才能把 `physical_first_article_gate` 从 HOLD 改为 PASS。
