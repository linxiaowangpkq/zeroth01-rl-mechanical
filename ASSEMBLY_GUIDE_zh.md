# Physical Mount V1 首件与装配流程

## 0. 目的

首件不是验证外观，而是关闭三项剩余风险：

1. STS3250 壳体 4×M2 实物孔位与开源 STS3215-family 承力件是否兼容；
2. 25T 舵盘、M3×6 输出固定和后轴支撑是否同轴；
3. 电缆出口、紧固工具空间和关节全行程是否可用。

在这三项完成前，仓库的整机打印状态保持 `HOLD`。

## 1. 首件量规

打印：

`generated/print/physical_mount_v1/first_article/STS3250_4XM2_FIRST_ARTICLE_FACE_GAUGE.stl`

建议：

- 0.4 mm 喷嘴，0.16–0.20 mm 层高；
- 量规平放，孔轴沿 Z；
- 不缩放；
- 打印后先用卡尺检查外形 49.22 × 28.72 × 3.00 mm；
- 2.2 mm 通孔只用于定位/装配检查，不代表最终螺纹底孔。

记录到 `reports/physical_mount_v1/first_article_measurements.csv`：

- 机身实际长、宽、总深度；
- 四孔中心距 X、Y；
- 螺纹有效深度；
- 输出轴到短端距离；
- 25T 花键配合；
- 后轴直径和突出量；
- 连接器/线缆出口包络。

判定：

- 四颗 M2 可徒手对正并逐步交叉锁紧，不允许用螺钉强行拉正量规；
- 量规不得明显翘曲；
- 输出轴和后轴实测值与报告一致后，才进入承力件首件。

## 2. 肩部首件

使用 S01/S02 与 S03/S06 所在源承力件打印一个完整肩部模块。

检查：

- 舵机壳体有明确止口/夹持面，不能只靠输出轴悬挂；
- 4×M2 可从设计方向装配并有工具空间；
- 舵盘连接父/子承力件，后轴支撑承担径向载荷；
- 线缆不进入肩 yaw/pitch 扫掠体；
- 手动断电旋转覆盖 guarded limits，无硬接触；
- 低电流 jog 后方向、零位与 URDF 一致。

## 3. 髋部首件

使用 S07/S08、S11/S12、S04/S05 所在承力件打印单侧髋模块。

检查：

- 三根关节轴的相交/偏置与 CAD 一致；
- hip-roll 左右壳体可沿输出轴采用不同朝向，但轴线必须镜像；
- 原始 hip-pitch 约 1.48 mm 的左右偏差必须决定是保留、修正承力件还是重建左右共用参数化基准；
- 最大屈伸时壳体、法兰、螺钉头和线束无干涉。

## 4. 装配原则

- 舵机壳体固定在 `owning_link`；
- 输出舵盘连接运动侧 link；
- 后轴支撑与输出轴同轴，不能让塑料悬臂单独承受弯矩；
- 结构件与外观壳分离；外观壳不得充当唯一承力件；
- 不把堵转扭矩当持续扭矩；
- 所有左右方向、零位、ID 通过逐个单舵机 jog 标定，不从 CAD 猜测。

## 5. 完成门

只有以下全部满足才允许整机打印：

- `first_article_measurements.csv` 无空白实测项；
- 量规 PASS；
- 肩部首件 PASS；
- 髋部首件 PASS；
- 16 个方向/零位/总线 ID 完成；
- 更新后 MuJoCo 中位姿、单关节 sweep、协调动作均无非相邻件穿透；
- 新 SolidWorks 装配不再使用任何 diagnostic envelope；
- 质量、质心和惯量由最终打印件与电子件实测回写。
