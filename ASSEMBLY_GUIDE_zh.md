# Zeroth-01 v3 RL-Fixed 首件装配指南

本指南只覆盖 v3 最终基线。不要一次性打印整机；先关闭 STS3250、双轴踝、CoreS3 支架和实物质量四项首件门禁。

## 1. 首件采购与量测

1. 采购 1 颗 FEETECH STS3250-C001 和 1 个 M5Stack CoreS3 K128。
2. 用 `sts3250_dimension_controlled.step` 与首件量规核对 `45.22×24.72×35 mm`、4×M2、25T/5.9 mm、M3×6 输出螺钉及后支撑位置。
3. 先加工/打印 1 套左右踝 roll 支架、horn 转接件和 7 mm 鞋底，确认材料、嵌件及层向后再复制。
4. 加工 2 mm 6061 CoreS3 U 型支架；用躯干上的 4×M3 通孔连接螺母板，不得钻削 CoreS3 外壳。

## 2. 骨架与 18 个舵机

1. 从已连接的 v2 承力骨架装配，不得用诊断 STEP 替代真实支架。
2. 按 `physical_mount_v3_rl_fixed_actuator_layout.json` 的 S01–S18 逐个安装，蓝色壳体中心、输出轴和 owner link 必须一致。
3. 左右肩、肘、髋、膝、踝必须按镜像变换装配；禁止为了视觉对称随意旋转舵机。
4. S17/S18 踝 roll 轴位于对应踝 pitch 轴下方 30 mm；父支架包住舵机壳，horn/脚属于旋转子链。
5. 安装 7 mm 鞋底并确认两脚齐平，底面不得翘曲。

## 3. CoreS3 与胸腔电子件

1. CoreS3 从躯干前侧装入 54.6 mm 受控开口，主机中心为 `(38.75,0,18) mm`，不安装脖子或额外前面板。
2. U 型支架从后侧托住主机，使用 4×M3 螺钉、垫圈和螺母板；保留 USB-C 插拔和线束弯曲空间。
3. 计算模块、电池和躯干 IMU 固定在胸腔内部；普通外观不得看到橙/紫/绿色模块。
4. CoreS3 是固定载荷，步行模型不含机械 pan/tilt。

## 4. SolidWorks 与运动验收

1. 打开 `OPEN_FIRST_ZEROTH01_V3_RL_FIXED_CONNECTED_WHITE_18_BLUE_STS3250.SLDASM`，确认 51/51 组件解析。
2. 普通视图检查连接、白色骨架、18 个蓝色舵机和黑色鞋底；透视视图检查 CoreS3 支架、计算、电池和 IMU。
3. 运行原生干涉检查：跨组件 `physical_interference_count` 必须为 0；同一旧躯干多实体联合重叠和非制造显示层单独记录。
4. 逐关节慢速扫限位，检查线束、螺钉头、horn、脚底和相邻外壳；仓库中的 64 帧 GIF 只是数字样本，不替代首件扫角。

## 5. RL 与真机放行

1. 训练只使用 canonical URDF/MJCF，不使用 v1/v2 或诊断 STEP 作为动力学真值。
2. 采用 handoff 中的质量、惯性、阻尼、摩擦和电机强度随机化。
3. STS3250 连续设计上限为 1.2552512 N·m；当前准静态峰值 1.414238 N·m，因此必须输出逐轴峰值/RMS 扭矩、电流和热轨迹。
4. 完成总线 ID、机械零位、方向、实物质量/质心/惯量识别后，才允许更新为 as-built 模型。
5. 真机顺序：单舵机量规 → 双轴踝台架 → 吊架慢速站立 → 低幅步态 → 热稳态复测；任一步超温、过流或失稳即回退。
