# Zeroth-01 round-v1 集成与装配指南

## 先给结论

当前交付已经把“舵机只摆在关节旁边”的错误语义改成了明确的两侧传动链：

`父 link / 骨架 → 父侧舵机笼 → STS3250 壳体 → 双输出轴/采购 25T 舵盘 → 子侧前后转接盘 → 子 link`

SolidWorks 的 48 个 lower/zero/upper 姿态逐项验证了：父侧舵机壳体不跟随自身关节转动、子侧输出件按指令角度转动、两侧轴心重合。证据在
`reports/solidworks_round_v1_transmission_semantics.csv`。

这解决了仿真和装配语义，但还不是“下载后全部打印即可行走”的生产放行。公开上游的 17 个 link 是聚合开放表面 STL，缺少可追溯的支架孔位、舵盘附件孔系、反侧轴承和紧固件定义；因此本交付提供可修改 STEP、SolidWorks 便携装配体和 3 个非承力试装 STL，并把生产承力件限定为采购舵盘实测后再冻结的 CNC 6061-T6 候选件。

## 组件放置

所有坐标均相对 `Torso`，单位为 mm；机器人前方是 `-Y`，上方是 `+Z`。

| 组件 | 中心 XYZ | 包络 XYZ | 名义质量 | 安装位置 |
|---|---:|---:|---:|---|
| 相机模组 | `[0, -30, 108]` | `65 × 20 × 18` | 35 g | 黑色面罩后方，双镜头窗中心 `X=±27` |
| IMU | `[0, 8, 10]` | `32 × 25 × 8` | 12 g | 胸腔中心附近、计算托盘上，轴与 `Torso` 对齐 |
| 计算/电源板 | `[0, 22, 15]` | `105 × 20 × 70` | 180 g | 后胸可维护托盘 |
| 3S2P 电池+BMS | `[0, 0, -44]` | `75 × 38 × 38` | 340 g | 胸腔下部居中，降低质心 |

这些是用于 RL 和布置检查的 `ASSUMED_FOR_RL` 包络，不是已经选型的采购件。最终相机、计算板、IMU、电芯、BMS、稳压器和连接器确定后，必须称重、测安装质心并重新生成 URDF/MJCF 惯量。

MuJoCo 还包含：

- 固定相机与 REP-103 `camera_optical_frame`；
- IMU 加速度、角速度传感器；
- 左右脚各前/后两个压力触点，共 4 个；
- 16 个关节位置/速度/执行器通道。

## 文件和颜色含义

| 外观 | 含义 |
|---|---|
| 金属灰 | 厂商 STS3250 STEP 的真实外壳实例，不是悬空黑点 |
| 奶油色关节笼 | 父侧承受舵机反扭矩的 `SERVO_CAGE` |
| 青绿色圆盘 | 跟随子 link 的前/后输出转接盘 |
| 棕色胸腔件 | 内部主脊、电子托盘/包络 |
| 奶油色大壳 | 可拆外观壳；通过分缝 boss 围绕内部结构安装 |

便携装配入口：

`generated/solidworks/portable_flat_round_v1/OPEN_FIRST_ZEROTH01_ROUND_V1_WITH_STS3250.SLDASM`

同一目录包含 36 个 `SLDPRT`，打开时不依赖本机绝对路径。

## 舵机关节装配顺序

1. 采购实际 STS3250 和随附/选配 25T 舵盘；确认输出花键外径 5.9 mm、25T、中心 M3 固定，但不要把未知的齿形或附件孔距当作图纸事实。
2. 用 `ZEROTH01_ROUND_V1_SERVO_CAGE.step` 做父侧笼试制；先检查厂商 STEP 总包络 `45.220 × 37.400 × 24.720 mm` 与实物，笼体名义单边间隙 0.6 mm。
3. 将舵机壳体固定到父侧笼，使电机壳体对父 link 刚性反扭；输出轴中心必须与 URDF 关节原点同心。
4. 测量采购舵盘孔距。前/后转接盘的四个径向 M3 槽覆盖候选 `11–20 mm` PCD；只有实测匹配后才冻结孔位和紧固扭矩。
5. 将采购舵盘用中心 M3 紧固到 25T 输出轴，再把舵盘固定到
   `OUTPUT_HUB_FRONT/REAR`；不要直接 3D 打印花键。
6. 用转接盘自有的 `29 mm PCD` 把前/后盘固定到子侧叉架；反侧必须设置同轴支撑/轴承，禁止只靠单侧塑料悬臂承载。
7. 手动转动整个机械范围，检查螺钉头、线束、笼体、子侧叉架和壳体的动态间隙，再做低扭矩台架联动。

当前 STEP 已通过单/双实体、正体积和厂商包络门禁；3 个最终试装 STL 已通过单组件、闭合、流形、绕向和 STEP/STL 体积一致性门禁。它们仅用于尺寸试装，不能作为步行承力件批准。

## 躯干、电子舱和外壳装配顺序

1. 以 `TORSO_SPINE.step` 为内部基准件，先固定肩部横梁、髋部横梁和颈部座；外观壳不作为唯一承力路径。
2. 在后胸维护侧安装计算/电源托盘；IMU 靠近躯干中心并与 `Torso` 轴对齐。
3. 将电池置于胸腔下部居中位置，使用机械限位和阻燃隔离，不允许只用胶粘；预留主保险、总开关、BMS、充电口和快速断电。
4. 将相机固定在黑色面罩后，镜头对准两个青绿色窗口；装配后标定相机内参和 `camera_optical_frame → Torso` 外参。
5. 先装后壳，再装前壳；胸、头、骨盆前后壳通过分缝 boss 可拆连接。线束必须走内部主脊侧面并保留每个旋转关节的服务环。
6. 左右鞋底先各打印一件做试装；8 mm 加厚鞋底通过两条 12 mm 绑带槽固定，未完成拉脱、扭转、疲劳和落脚冲击试验前禁止行走。

## 上电与标定顺序

1. 断开舵盘负载，逐个确认 16 台舵机的唯一 bus ID。
2. 回填 `generated/config/zeroth01_hardware_calibration_template.csv` 中的方向符号、零位、硬限位、回差和死区。
3. 机械中位安装舵盘，先限制到低电流/低扭矩，逐轴验证正方向。
4. 执行无负载 48 姿态联动，再执行支撑架上的全身低速联动。
5. 记录母线压降、电流、壳温、跟踪误差和急停行为；随后才允许落地。
6. RL 首次实机策略使用 `1.2552512 N·m` 连续扭矩上限；是否能走必须由训练轨迹的扭矩-速度、RMS 电流、温升、足底冲击和滑移数据决定，不能由静态门禁代替。

## 放行状态

| 项目 | 状态 |
|---|---|
| URDF/MJCF RL 机械描述 | PASS |
| MuJoCo 16 轴运动与离散自碰撞门禁 | PASS |
| SolidWorks 父壳体/子输出传动语义 | PASS |
| 新 GIF 和三视图 | PASS |
| 11 个外观/鞋底最终 STL | PASS（非承力范围） |
| 3 个接口试装最终 STL | PASS（仅配合检查） |
| CNC 承力接口生产放行 | BLOCKED：待实物舵盘、轴承、紧固件、线束和公差 RFQ |
| 打印后直接拼装行走 | NOT READY |
