# Zeroth-01 最小可靠圆润版装配指南

## 先决条件

本指南假设已有一套与 canonical Zeroth-01 17-link 几何一致、可自由运动的原始骨架。当前仓库提供的是圆润外壳、脚底、头部器件包络和 RL 数据，不包含一套已经工程签字的全新承载骨架。

禁止把 S01–S16 彩色圆片打印后夹在关节里；它们只用于图纸和审阅视图。

## 装配层级

1. **承载层**：原 Zeroth-01 骨架、原关节支架/输出接口、轴承和紧固件。
2. **电子层**：主控托盘、IMU、电池+BMS、屏幕、相机、ToF、线束和足底压力传感器。
3. **外观层**：前后胸壳、前后骨盆壳、前后头壳、面罩/口鼻件、左右加厚脚底。

先独立确认承载层无干涉，再装电子层，最后装外观层。任何外壳都不得承担关节输出力矩。

## S01–S16 关节身份

| ID | canonical joint | 颜色 |
|---|---|---|
| S01 | right_shoulder_pitch | `#E6194B` |
| S02 | left_shoulder_pitch | `#3CB44B` |
| S03 | right_shoulder_yaw | `#4363D8` |
| S04 | right_hip_pitch | `#F58231` |
| S05 | left_hip_pitch | `#911EB4` |
| S06 | left_shoulder_yaw | `#42D4F4` |
| S07 | right_hip_yaw | `#F032E6` |
| S08 | left_hip_yaw | `#BFEF45` |
| S09 | right_elbow_yaw | `#FABED4` |
| S10 | left_elbow_yaw | `#469990` |
| S11 | right_hip_roll | `#DCBEFF` |
| S12 | left_hip_roll | `#9A6324` |
| S13 | right_knee_pitch | `#FFE119` |
| S14 | left_knee_pitch | `#000075` |
| S15 | right_ankle_pitch | `#FF6F61` |
| S16 | left_ankle_pitch | `#006B3C` |

精确轴线、原点和守护限位见 `reports/joint_servo_frames.csv`。总线 ID、零位 count 和正方向仍必须通过实物标定，不能由颜色推断。

## 电子件安装区

坐标约定：Torso 原点，前方 `-Y`，上方 `+Z`，机器人左侧 `+X`。

| ID | 部件 | Torso 中心位置（m） | 状态 |
|---|---|---|---|
| D01 | Waveshare 双眼屏 | `[0, -0.042, 0.115]` | 型号和 STEP 已选 |
| C01 | Camera Module 3 Wide | `[0, -0.040, 0.091]` | 型号和 STEP 已选 |
| T01 | VL53L5CX ToF 载板 | `[0.031, -0.042, 0.092]` | 芯片已选，载板假设 |
| I01 | 躯干 IMU | `[0, 0.008, 0.010]` | 包络假设 |
| P01 | 主控/电源托盘 | `[0, 0.022, 0.015]` | 包络假设 |
| B01 | 3S2P 电池+BMS | `[0, 0, -0.044]` | 包络假设 |
| F01–F04 | 每只脚前/后压力位 | 见 layout JSON | 位置假设 |

完整尺寸、质量范围、光学帧和颜色见 `generated/config/round_v1_electronics_sensor_layout.json`。

## 推荐装配顺序

1. 将原骨架置于支架上，不装外壳，手动逐关节走完整守护范围。
2. 扫描 16 个舵机总线 ID；一次只使能一个关节，以低扭矩点动确认正方向。
3. 按 `zeroth01_hardware_calibration_template.csv` 记录零位、硬限位、背隙和空载电流。
4. 安装低位居中的电池托架，再装后胸主控托盘；保留总电源、保险丝和急停可达性。
5. 将 IMU 刚性固定于胸部中心附近，轴线对齐 Torso；软泡棉只可用于隔离高频振动，不能让板卡自由摆动。
6. 在头部前壳内安装 D01、C01、T01；先做相机/ToF 视场检查，再装曲面面罩。
7. 预布线并做全关节慢速扫掠；任何线束不得跨越输出盘、夹点或小于厂家最小弯曲半径。
8. 装左右加厚脚底和 F01–F04；确保压力位不被刚性壳体短路承载。
9. 装胸、骨盆和头部前后壳；逐片检查 0.5–1.0 mm 动态间隙和拆装可达性。
10. 在吊架上重新执行零位、站立位和单关节扫掠，最后才进入双腿低速测试。

## 紧固与打印件边界

- `generated/config/round_v1_fastener_bom.csv` 是占位/规划 BOM，不是已冻结采购单。
- 当前 STL 没有获得承载强度签字，也未冻结热熔螺母、螺钉长度、垫片、轴承或螺纹嵌件。
- 打印前必须确定打印机、喷嘴、材料、层高、壁数、填充、摆放和支撑；仓库没有擅自假设某台打印机配置。
- 头壳、胸壳和脚底先做单件试装；不要一次打印全套后才检查接口。

## 通电前门禁

- 机械：每个关节能在守护范围内手动运动，无硬碰、夹线和松动。
- 电气：保险丝、急停、BMS、极性、绝缘和接地检查完成。
- 标定：16 个 confirmed ID、zero、direction 均已填写，无重复地址。
- 软件：命令限位与 URDF 一致，连续扭矩初始上限不超过 `1.2552512 N·m`。
- 安全：首轮必须使用吊架、低电流限制和可物理断电的操作员。

以上任一项不满足时，不进入落地步行。
