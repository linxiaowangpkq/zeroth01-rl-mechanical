# v3 RL-Fixed 首件装配指南

本指南只覆盖 v3。不要一次性打印整机；先关闭 STS3250、双轴踝和 K151 转接板三项首件门禁。

## 0. 首件

1. 采购 1 颗 FEETECH STS3250-C001 和 1 套 M5Stack StackChan K151。
2. 用 `sts3250_first_article_gauge.stl` 检查舵机 45.22 × 24.72 × 35 mm 壳体、36.5 mm 总高参考、4×M2、25T/Ø5.9 输出、后轴和线束出口。
3. 打印左右踝横滚桥架、输出盘转接件和一只轻量加厚脚底；手动装配 S15/S16 踝俯仰与 S17/S18 踝横滚，确认轴距 50.000 mm。
4. 加工 66 × 60 × 3 mm 6061 K151 转接板；验证 48 × 32 mm M3 孔矩、躯干四条闭口槽、USB-C 和线束空间。
5. 任一首件不合格就回到源 CAD 修改，禁止批量加工。

## 1. 下肢

1. 从髋部向下安装：S07/S08 髋 yaw → S11/S12 髋 roll → S04/S05 髋 pitch → S13/S14 膝 pitch → S15/S16 踝 pitch。
2. 左右件保留 v2 的真实六维装配方向，不得为了视觉对称翻转电机；左右关节数值限位也不同，以 URDF 为准。
3. 在 S15/S16 输出侧安装白色踝横滚父桥架。桥架薄连接板必须位于舵机输出法兰外侧。
4. 把 S17/S18 舵机固定在桥架内，轴心位于踝俯仰轴正下方 50 mm；舵机壳属于父桥架，蓝色输出盘/转接件属于脚。
5. 安装 9 mm 轻量加厚脚底，确认两脚齐平。SolidWorks 验收要求机械相交体积为 0。

## 2. 躯干、手臂与固定 Q 手

1. 保留 v2 承力骨架，依次安装 S03/S06 肩 yaw、S01/S02 肩 pitch、S09/S10 肘轴。
2. Q 手是固定末端，没有灵巧手自由度；旧爪组件数量必须为 0。
3. 不安装已删除的大胸前外观板。橙色计算模块、紫色电池和绿色躯干 IMU 使用透视装配图中的位置，并保留检修、散热和线束空间。

## 3. K151 采购头

1. 先用四条躯干闭口槽把 3 mm 转接板临时固定在主躯干顶部。
2. 用 4×M3 将 K151 成品底座固定到 48 × 32 mm 孔矩；不得钻削 K151 外壳。
3. 头部直接落在转接板上，连接链必须为“躯干 → 转接板 → K151”，不得新增长脖子或恢复旧胸前板。
4. USB-C/通信线沿躯干背侧走线，避开肩轴扫掠区并留应变释放。
5. 步行时锁定 K151 内部 pan/tilt 中位，将完整 187 g 模块视作固定负载；非步行交互阶段才解锁。

## 4. 电气、传感器与标定

- 主躯干 IMU、K151 摄像头/双麦克风/扬声器/头部 IMU 使用 `rl_handoff.json` 中的 frame。
- 每只脚的 4 个接触 frame 用于仿真；实机 FSR/足底传感器选型后再建立标定映射。
- 每装一个舵机都用限流电源低速点动，记录 bus ID、中位计数、方向符号、软限位和无负载电流；回填 `actuator_layout.json` 的待标定字段。
- 12 V 总线按实测电流、线长和压降选线；不得以 18×堵转电流作为常态功耗，也不得忽略熔断、急停和分支保护。

## 5. 最终验收

1. 打开 SolidWorks 正常装配，确认 51/51 组件；再打开透视装配核对 18 个蓝色舵机、橙色计算模块、紫色电池和绿色 IMU。
2. 运行 SolidWorks 原生干涉脚本；只允许 K151 显示参考层重叠，机械干涉必须为 0。
3. 运行 FK、URDF、MJCF、逐轴限位、64 帧协调运动和准静态扭矩门禁。
4. 实机称量各总成并测量质心/惯量，回填 URDF/MJCF 后重新训练或做域随机化。
5. RL rollout 必须记录 18 轴峰值/RMS 扭矩、速度、电流和热量。名义准静态峰值 1.1864 N·m 已通过，但不等于动态步行签核。
6. 只有 `MASS_IDENTIFIED_PASS`、`STS3250_FIRST_ARTICLE_PASS`、`STACKCHAN_ADAPTER_FIRST_ARTICLE_PASS` 和动态扭矩/热门禁全部关闭，才可宣称实物机械发布。
