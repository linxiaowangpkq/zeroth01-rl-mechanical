# 已否决实验：替代舵机/笼体一体化路线

## 状态

`REJECTED_NOT_SELECTED`

该 revision 曾尝试把 16 个替代 STS3250、parent-side 笼体和 child-side 输出 hub 叠加到公开 Zeroth-01 聚合网格上。它不属于当前交付，不得用于 RL、制造或安装判断。

## 否决根因

1. step.parts 文件内部标识为 `ST-3235M-20211119-A_ASM`，不是当前 `ST-3250-C001`。
2. 该 STEP 的输出轴为局部 `+Y`；旧实验按 `+Z` 放置，造成用户看到的舵机、孔位、齿轮/圆盘冲突。
3. 公开 17-link 网格已经聚合原机构，缺少可追溯的舵机安装面、轴承、舵盘、紧固件和公差；在其表面“补画”笼体无法证明真实反力路径。
4. 新增 16 个舵机实体还会产生重复质量/惯量风险，并破坏已验证的最小机械基线。

## 当前替代方案

- 保留原 17-link 连接、轴线、零位和守护范围。
- S01–S16 只在原关节 frame 放置不同颜色的非物理标识。
- SolidWorks 当前总装明确要求替代舵机、笼体、hub 计数均为 0。
- STS3250-C001 规格只进入执行器元数据；真实安装待供应商可追溯 CAD 或实物量测。

当前设计与门禁见 `DESIGN_LEDGER.md`、`README_zh.md` 和 `reports/solidworks_round_v1_gate.json`。
