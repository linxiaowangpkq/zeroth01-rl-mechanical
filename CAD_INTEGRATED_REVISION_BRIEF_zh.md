# Zeroth-01 round-v1.1 一体化修复 CAD brief

## 模型与目标

- 模型：`zeroth01_round_v1_1_integrated`，在不改变现有 18-link / 16-actuated-DoF
  关节树、轴线、限位和 RL 安全盒的前提下修复 round-v1 的机械表达。
- 主输出：STEP；次输出：SolidWorks SLDPRT/SLDASM、打印 STL、URDF、MJCF、
  PNG/GIF 和审计报告。
- 单位：CAD/打印为 mm；URDF/MJCF 为 m、kg、s、rad。
- 坐标：沿用现有 Torso/link frame；CAD 中 `+Z` 向上，机器人前方为 `-Y`。

## 根因修复

- 旧脚本把 STS3250 壳体绑定到 joint child link；因此壳体和输出端一起转，只能表示
  轴线占位，不能表示扭矩反力和传动。
- 修复后：
  - STS3250 壳体及承力笼绑定到 parent link 的 joint frame；
  - 25T 输出盘/双轴支撑轮毂绑定到 child link；
  - Motion 中必须测得 `housing -> output` 的相对转角等于关节命令角；
  - 每个舵机笼通过同轴双侧环、背桥和 parent-side 安装耳表达反力路径。

## STS3250 接口

- 真实输入：`source_assets/vendor/sts3250/FEETECH_STS3250.step`。
- STEP 局部包络：约 `45.220 × 37.400 × 24.720 mm`；输出轴为局部 `Z`。
- 厂商规格：`25T / OD 5.9 mm` 花键，中心固定螺钉 `M3×6`，双轴。
- 舵机笼内腔相对 STEP 包络名义增加 `0.6 mm/side`，外壁 `2.8 mm`。
- 输出盘使用采购的 25T 舵盘/花键适配器；本 CAD 只表达 OD5.9、M3 固定轴、
  输出盘和对侧轴承包络，不伪造未取得的花键齿形或舵盘孔系。
- 舵机笼/输出盘是 CNC 6061 或金属样件候选，不作为普通 FDM 承力件放行。

## 外观曲率

- 胸壳外包络维持 `170 × 76 × 130 mm`，主圆角由 18 mm 增至 30 mm。
- 头壳外包络维持 `132 × 66 × 78 mm`，主圆角由 25 mm 增至 31 mm；耳部改为
  更低、更宽的融合圆角体。
- 骨盆壳主圆角由 15 mm 增至 22 mm。
- 保留 2.4 mm 名义壳厚、0.35 mm/side 前后壳分缝及肩/髋动态 keep-out。
- 真实舵机改为金属灰；奶油色舵机笼与青色输出盘明确显示“壳体固定、输出端转动”。

## 电子舱布局与 RL 假设

| 模块 | Torso 局部中心 xyz (mm) | 包络 (mm) | RL 名义质量 |
|---|---:|---:|---:|
| RGB-D/双目相机舱 | `0, -30, 108` | `65 × 20 × 18` | 0.035 kg |
| IMU | `0, 8, 10` | `32 × 25 × 8` | 0.012 kg |
| 计算板通用托盘 | `0, 22, 15` | `105 × 20 × 70` | 0.180 kg |
| 3S2P 18650 电池+BMS包络 | `0, 0, -44` | `75 × 38 × 38` | 0.340 kg |

- 相机在头部黑色 visor 后；新增 optical frame，视轴沿机器人前方 `-Y`。
- IMU 固定在胸部中心脊柱附近，轴线与 Torso frame 对齐。
- 计算板位于后胸可拆盖内；电池位于下胸中央，降低质心并保持左右对称。
- 足底每侧布置前/后两个压力传感 site，供 MuJoCo touch/足接触观测。
- 以上型号尚未冻结，包络和质量均标记为 `ASSUMED_FOR_RL`；实机选型和称重后必须
  重生成质量、质心和惯量。

## 装配与验证目标

- SolidWorks：17 个源 link、圆润壳/脚底、16 个 STS3250、16 个 parent-side
  舵机笼、16 个 child-side 输出盘及电子模块均可定位。
- 传动：48 个 lower/zero/upper 姿态中，每个关节的壳体相对 parent 固定，输出盘
  相对壳体转角误差不超过 `1e-6 rad`。
- Motion GIF：重新生成，不复用旧 round-v1 动画。
- 几何：新增 STEP 为闭合正体积 B-Rep；打印件重新通过水密和体积一致门禁。
- URDF/MJCF：新增相机、IMU、计算板、电池固定 link/frame、足底触觉 site，并重新
  核对总质量、惯量、16DoF、随机姿态与角点碰撞。
- 真实性边界：通过表示数字装配和采样运动；花键齿形、采购舵盘孔系、金属件强度、
  线束弯曲半径和打印/CNC 公差在实物冻结前仍不得标记为量产放行。
